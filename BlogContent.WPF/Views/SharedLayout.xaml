<UserControl x:Class="BlogContent.WPF.Views.SharedLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BlogContent.WPF.Views"
             xmlns:converters="clr-namespace:BlogContent.WPF.Utilities.Converters" 
             xmlns:viewmodel="clr-namespace:BlogContent.WPF.ViewModel" 
             xmlns:base="clr-namespace:BlogContent.WPF.ViewModel.Base" 
             d:DataContext="{d:DesignInstance Type=viewmodel:UserProfileViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BooleanToHighlightConverter x:Key="BooleanToHighlightConverter"/>

        <SolidColorBrush x:Key="LayoutBackgroundBrush" Color="#F5F6FA"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SurfaceMutedBrush" Color="#F3F4F6"/>
        <SolidColorBrush x:Key="BorderLightBrush" Color="#E3E6EA"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#6B7280"/>
        <SolidColorBrush x:Key="TitleTextBrush" Color="#1F2937"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderLightBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Effect" Value="{StaticResource ShadowEffect}"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TitleTextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="MetaTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
        </Style>

        <Style x:Key="BodyTextStyle" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        </Style>

        <Style x:Key="SmallAvatarStyle" TargetType="Border">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="#D1D5DB"/>
        </Style>

        <Style x:Key="LargeAvatarStyle" TargetType="Border" BasedOn="{StaticResource SmallAvatarStyle}">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="CornerRadius" Value="40"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
        </Style>

        <Style x:Key="OutlineButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
        </Style>

        <Style x:Key="GhostButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SurfaceMutedBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TitleTextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="NavItemStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="EmptyStateTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource LayoutBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель навигации -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Height="72" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Логотип и название -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="BlogPlatform"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="{StaticResource TitleTextBrush}"/>
                    <TextBlock Text="by. Gavrs"
                               FontSize="12"
                               Foreground="{StaticResource MutedTextBrush}"
                               Margin="10,4,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <TextBox Grid.Column="1"
                         Width="320"
                         Height="36"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         Text="Поиск авторов"
                         Foreground="{StaticResource MutedTextBrush}"
                         Background="{StaticResource SurfaceMutedBrush}"
                         BorderBrush="{StaticResource BorderLightBrush}"
                         BorderThickness="1"
                         Padding="12,0"
                         IsReadOnly="True"/>

                <!-- Меню пользователя -->
                <StackPanel Grid.Column="2" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">

                    <!-- Кнопка создания поста -->
                    <Button Content="Создать пост" 
                            Command="{Binding CreatePostCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Margin="0,0,15,0"/>

                    <!-- Аватар пользователя (исправленный круглый) -->
                    <Border Style="{StaticResource SmallAvatarStyle}"
                            Cursor="Hand">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" 
                                         Command="{Binding ViewProfileCommand}"/>
                        </Border.InputBindings>
                        <Image Source="{Binding UserProfilePictureUrl, Converter={StaticResource MediaUrlToImageConverter}, ConverterParameter=avatar_small}" 
                               Stretch="UniformToFill" 
                               RenderOptions.BitmapScalingMode="HighQuality">
                            <Image.Clip>
                                <EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/>
                            </Image.Clip>
                        </Image>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Контент страницы -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Левая панель -->
            <StackPanel Grid.Column="0" Margin="20,20,10,20">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <!-- Мини-профиль -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <!-- Аватар пользователя -->
                            <Border Style="{StaticResource SmallAvatarStyle}">
                                <Image Source="{Binding UserProfilePictureUrl, Converter={StaticResource MediaUrlToImageConverter}, ConverterParameter=avatar_small}" 
                                       Stretch="UniformToFill">
                                    <Image.Clip>
                                        <EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/>
                                    </Image.Clip>
                                </Image>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding UserFullName}" 
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TitleTextBrush}"/>
                                <TextBlock Text="Ваш профиль" 
                                           Style="{StaticResource MetaTextStyle}"/>
                            </StackPanel>
                        </StackPanel>

                        <Button Content="Мой профиль"
                                Style="{StaticResource OutlineButtonStyle}"
                                Command="{Binding ViewProfileCommand}"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="Навигация" 
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource MutedTextBrush}"
                           Margin="4,4,0,10"/>

                <Border Style="{StaticResource NavItemStyle}"
                        Cursor="Hand"
                        Background="{Binding IsHomePage, Converter={StaticResource BooleanToHighlightConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🏠" FontSize="16"/>
                        <TextBlock Text="Главная"
                                   Margin="10,0,0,0"
                                   FontSize="14"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <Border.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding NavigateToHomeCommand}"/>
                    </Border.InputBindings>
                </Border>

                <Border Style="{StaticResource NavItemStyle}"
                        Cursor="Hand"
                        Background="{Binding IsProfilePage, Converter={StaticResource BooleanToHighlightConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="👤" FontSize="16"/>
                        <TextBlock Text="Профиль"
                                   Margin="10,0,0,0"
                                   FontSize="14"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <Border.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding ViewProfileCommand}"/>
                    </Border.InputBindings>
                </Border>

                <Border Style="{StaticResource NavItemStyle}"
                        Cursor="Hand"
                        Background="{Binding UserPostsPage, Converter={StaticResource BooleanToHighlightConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📝" FontSize="16"/>
                        <TextBlock Text="Мои публикации"
                                   Margin="10,0,0,0"
                                   FontSize="14"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <Border.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding NavigateToMyPostsCommand}"/>
                    </Border.InputBindings>
                </Border>

                <Border Style="{StaticResource NavItemStyle}"
                        Cursor="Hand"
                        Background="{Binding UserLikesPage, Converter={StaticResource BooleanToHighlightConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="❤️" FontSize="16"/>
                        <TextBlock Text="Понравившиеся"
                                   Margin="10,0,0,0"
                                   FontSize="14"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <Border.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding NavigateToLikesCommand}"/>
                    </Border.InputBindings>
                </Border>

                <Button Content="Создать пост"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding CreatePostCommand}"
                        Margin="0,8,0,4"/>

                <Border Style="{StaticResource NavItemStyle}"
                        Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🚪" FontSize="16"/>
                        <TextBlock Text="Выход"
                                   Margin="10,0,0,0"
                                   FontSize="14"
                                   Foreground="{StaticResource MutedTextBrush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <Border.InputBindings>
                        <MouseBinding MouseAction="LeftClick" Command="{Binding LogoutCommand}"/>
                    </Border.InputBindings>
                </Border>
            </StackPanel>

            <!-- Содержимое страницы -->
            <ContentControl Grid.Column="1" 
                    Content="{Binding RelativeSource={RelativeSource AncestorType=local:SharedLayout}, Path=PageContent}" 
                    Margin="10,20,20,20"/>
        </Grid>
    </Grid>
</UserControl>
